const PREFIX="app-financas-fx:";
function latestQuote(){let latest=null;for(let i=0;i<localStorage.length;i++){const key=localStorage.key(i);if(!key?.startsWith(PREFIX))continue;try{const q=JSON.parse(localStorage.getItem(key));if(q?.savedAt&&(!latest||q.savedAt>latest.savedAt))latest=q;}catch{}}return latest;}
function fmtDateTime(ms){const d=new Date(Number(ms));if(Number.isNaN(d.getTime()))return"";return `${d.toLocaleDateString("pt-BR",{day:"2-digit",month:"2-digit",year:"numeric"})} às ${d.toLocaleTimeString("pt-BR",{hour:"2-digit",minute:"2-digit",second:"2-digit"})}`;}
function annotate(){const q=latestQuote(),text=q?`Cotação consultada em ${fmtDateTime(q.savedAt)}${q.provider?` · ${q.provider}`:""}`:`Cotação consultada nesta sessão`;for(const id of ["saldoInfoDetalhes","receitasInfoDetalhes","despesasInfoDetalhes","resultadoInfoDetalhes"]){const box=document.getElementById(id);if(!box)continue;let line=box.querySelector(".quote-time-reference");if(!line){line=document.createElement("div");line.className="balance-info-reference quote-time-reference";box.appendChild(line);}line.textContent=text;}}
const dashboard=document.getElementById("dashboard");dashboard?.addEventListener("pointerenter",e=>{if(e.target.closest?.(".info-button"))setTimeout(annotate,20);},true);dashboard?.addEventListener("click",e=>{if(e.target.closest?.(".info-button"))setTimeout(annotate,20);});window.addEventListener("period-change",e=>{if(e.detail?.viewId==="dashboard")setTimeout(annotate,120);});setTimeout(annotate,300);
